# Папка /data на проде наполняется скриптом scripts/export-catalog-json.sh (VPS).
# В репо лежит пустой — catalog.json НЕ коммитим (генерируется на сервере).
# ВАЖНО: rsync деплоя не должен удалять /var/www/dsom-frontend/data/
# (либо --exclude=data/, либо перегенерация сразу после rsync — см. подсказку в экспортёре).
